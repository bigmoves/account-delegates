{
  "lexicon": 1,
  "id": "com.atproto.server.checkDelegate",
  "defs": {
    "main": {
      "type": "query",
      "description": "Served by an account's managing app. Asked by the account's PDS, authenticated with service auth signed by the account, whether a DID may write as the account and how far.",
      "parameters": {
        "type": "params",
        "required": ["account", "did"],
        "properties": {
          "account": { "type": "string", "format": "did" },
          "did": { "type": "string", "format": "did" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["permissions"],
          "properties": {
            "permissions": { "type": "array", "items": { "type": "string" }, "description": "Permission strings in auth-scope syntax. Empty when the DID is not a delegate." },
            "expiresAt": { "type": "string", "format": "datetime", "description": "How long the PDS may cache this answer." }
          }
        }
      }
    }
  }
}
